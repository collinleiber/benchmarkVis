shiny.help = list(
    "data.format" = c("Appropriate data format", "The app expects your data to be a benchmarkVis compatible datatable. Alternatively you can use one of the provided wrappers, e.g. for mlr benchmark results")

)